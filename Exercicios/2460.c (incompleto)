#include <stdio.h>
#include <stdlib.h>

struct cel {
    int valor;
    struct cel *next;
};
typedef struct cel celula;

void inserir(int y, celula *p){
    celula *nova = (celula *)malloc(sizeof(celula));
    nova->valor = y;
    nova->next = p->next;
    p->next = nova;
}

void busca_remove ( int x, celula *p){
    celula *q= p->next;
    while(q != NULL && q->valor != x){ 
        p=q;
        q=q->next;
    }
    if (q!=NULL)
    p->next = q->next;
    free(q);

}

int main(void){
    int n, m, num;
    celula *p;
    scanf("%d", n); /*n é o numero de pessoas na fila e m o numero de pessoas que saem da fila*/
    int v[m];

    for(int i=0; i<n; i++){
        scanf("%d", &num);
        inserir(num, p);
    }
    scanf("%d",&m);

    for(int i=0; i<m; i++){
        scanf("%d",&v[m]);
    }
    for(int i=0;i<;i++){
        
    }

    return 0;
}
